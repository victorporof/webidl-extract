# webidl-extract

```sh
git clone https://github.com/victorporof/webidl-extract.git
cd webidl-extract
yarn install # install dependencies
yarn build # clone and build specs
yarn start # create out/raw.json and out/props.json
```
